# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets. The bridge reads ELEVENLABS_API_KEY and friends from here, so a
# committed .env is a leaked key. .env.example is the template to copy.
.env
.env.*
!.env.example

# electron-builder output (installers and unpacked app)
release

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Copied out of node_modules by scripts/start.mjs — 34MB of build output.
public/mediapipe/
